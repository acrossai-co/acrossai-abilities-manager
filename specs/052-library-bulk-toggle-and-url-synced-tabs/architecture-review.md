# Architecture Review Report — Feature 052 Plan

**Mode**: architecture
**Scope**: `specs/052-library-bulk-toggle-and-url-synced-tabs/plan.md` + `docs/planning/052-library-bulk-toggle-and-url-synced-tabs.md`
**Assessment date**: 2026-07-13
**Reference constitution**: `.specify/memory/CONSTITUTION.md` v1.4.8
**Reference memory**: `docs/memory/INDEX.md` (routing) + `specs/052-…/memory-synthesis.md`

## Semantic Models (built, not printed)

- **Boundary Model**: Feature 052 stays inside the Library module (`includes/Modules/Library/`) + the admin orchestrator (`admin/Main.php`) + the React admin surface (`src/js/ability-library/`). No cross-boundary leakage.
- **Contract Inventory**: 3 new JS named-export helpers, 1 new default-export hook, 3 new public PHP static helpers + 1 private, 1 new localized JS data key. All contract additions are additive; no existing contract is changed.
- **Task-Implementation Map**: Not yet applicable — `tasks.md` will be generated by `/speckit-tasks`.
- **Dependency Graph**: `LibraryPage.js` gains a new dependency on `useLibraryTabSync` (same module tree). `useLibraryTabSync` mirrors the existing `useUrlViewSync` (sibling module). PHP helpers add read-only dependencies on `AcrossAI_Ability_Library_Config` and `AcrossAI_Ability_Library_Registry` — both same-namespace siblings.

## Violations

| ID | Category | Severity | Location(s) | Summary | Evidence/Rationale |
|:---|:---|:---|:---|:---|:---|
| — | — | — | — | **No architecture violations detected.** | Plan stays inside pre-approved boundaries; no new hooks; no new modules; DEC-DESIGN-OVERRIDES-DATAVIEWS pre-approves the Library-page React-not-DataForm surface. |

## Consistency Risks

- **NONE at the boundary level.** All edits respect the Modular Architecture rule (§I), the Admin Partials Rule (admin/Main.php owns enqueue), the Boot Flow Rule (no new hooks — zero new `add_action`/`add_filter` calls), and the Namespace Rule (`AcrossAI_Abilities_Manager\Includes\Modules\Library`, `AcrossAI_Abilities_Manager\Admin` — both match directory paths).
- **Constitution §III (DataForm/DataViews mandate)** — deviation applies but is pre-approved via `DEC-DESIGN-OVERRIDES-DATAVIEWS`. This review confirms no NEW deviation is introduced; the two-button header row is a command-control surface (not form, not list), which sits outside §III's scope even before the deviation is invoked.

## Task Synchronization

- **Status**: N/A — `tasks.md` not yet generated. Re-run at `/speckit-tasks` completion via `/speckit-architecture-guard-governed-tasks`.
- **Missing Implementations**: None expected — every planned file has an existing sibling to model against.
- **Pending Tasks**: `tasks.md` produced by `/speckit-tasks`.

## Metrics

- **Constitution Compliance**: 100% (7 of 7 principles PASS; the one relevant deviation is pre-approved).
- **Boundary Integrity**: Strong. No cross-module leakage; no new hook seam; no new REST route.
- **Architectural Risk**: **LOW**. Feature adds three named JS helpers + one hook + three PHP statics + one SCSS block + one localized JS key. Every addition is inside an established boundary with an established sibling pattern to copy.

## Code Quality Findings (SonarLint) — SKIPPED

No SonarLint rules bundle detected in the extension source, and no `--sonarlint` opt-in surface is available for this run. Per skill fallback: skip step 7b, note here that the optional scan was unavailable, and proceed with the rest of the architecture review.

## Constraint Cross-Check (against memory-synthesis.md)

| Memory Entry | Feature 052 Compliance | Evidence |
|---|---|---|
| `DEC-META-ACROSSAI-NAMESPACE` (Active) | ✅ Respected | tab_group is read from `$args['meta']['acrossai']['tab_group']` via `push_definition()` — unchanged; Feature 052 consumes the JS-side derived `tabGroup` field. |
| `DEC-ABSORBED-CODE-INCLUDES-TIER` (Active) | ✅ Respected | `Ability_Definition` stays at `includes/Modules/Library/`; helpers are additive on the base class. |
| `DEC-LIBRARY-CATEGORY-SLUG-REBRAND` (Active) | ✅ Respected | The single `ability()` contract and `push_definition()` derivation flow are untouched. |
| `DEC-DESIGN-OVERRIDES-DATAVIEWS` (Active — Accepted Deviation) | ✅ Governed by pre-existing decision | The header row uses `<Button>` — command control, not form or list. |
| `DEC-NODE-20-BUILD-REQUIRED` (Active) | ✅ Enforced at build time | Bundle regeneration in CHANGE-8 must run on Node ≥ 20. |
| `AC-ENQUEUE-ADMIN` (Constitution §I) | ✅ Respected | `bulkToggleState` localization lands inside `Admin\Main::enqueue_scripts()`. |
| `AC-HOOKS-MAIN` (Constitution §I) | ✅ Respected — vacuously | Feature 052 adds ZERO new PHP hooks. |
| `AC-FILE-HEADER-PATTERN` | ✅ Planned | New `useLibraryTabSync.js` will carry the standard `@package` header per pattern. |
| `PATTERN-NAMED-EXPORT-JEST` | ✅ Applied to all four new JS helpers | `collectInScopeCategories`, `buildBulkPatch`, `computeInScopeBulkState`, `parseTabFromUrl`, `buildUrlFromTab` — every helper is a named export. |
| `PATTERN-PROTECTED-SLUGS-JS-LOCALIZE` | ✅ Applied for `bulkToggleState` | PHP→JS via existing localization channel. |
| `BUG-WP-LOCALIZE-SCRIPT-RENDER` | ✅ Guarded via CHANGE-2 placement rule | Localization inside `enqueue_scripts()`, never from render. |
| `BUG-CLASS-EXISTS-AUTOLOAD-FALSE-SILENT` | ✅ Guarded via CHANGE-1 wording | `class_exists()` uses default `autoload=on`. |
| `BUG-JEST-MOCK-LIST-STALENESS` | ✅ Guarded via CHANGE-6 test extension | `LibraryPage.test.js` mock allowlist to be updated. |

No hard conflicts. No soft conflicts.

## Refactor Tasks

None — no violations to refactor.

## Constitution Update Proposal

None. Constitution v1.4.8 is aligned with the plan. No cross-cutting drift observed.

## Action Plan

1. **Critical Fixes**: None.
2. **Architecture Alignment**: None needed.
3. **Code Quality**: None flagged.
4. **Durable Memory Preservation**: Not triggered — Feature 052 uses only existing durable patterns. Post-implementation `/speckit-memory-md-capture-from-diff` will re-check.
5. **Next Step**: Proceed to `/speckit-tasks`.
6. **Remediation**: None needed. Plan is architecture-ready.

## Framework Preset Guidance

No framework preset installed at `.specify/presets/architecture-guard-preset.md`. Framework-agnostic review applied — all boundaries evaluated against the AcrossAI Abilities Manager Constitution (WP-plugin-specific rules folded into that constitution).
